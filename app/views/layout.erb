<h1>My Movie Library</h1>

<nav>
  <% if logged_in? %>
  <ul>
    <a href='/users/<%=get_user_by_session.slug%>'>Home</a>
    <a href='/users/logout'>Logout</a>
    <a href='/movies'>Movies</a>
    <a href='/genres'>Genres</a>
  </ul>
  <% end %>
</nav>

<% if flash.has?(:message) %>
  <%= flash[:message] %>
  <% end %>

  <%=yield%>
